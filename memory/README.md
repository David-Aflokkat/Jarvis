# memory/

This directory stores persistent memory files generated by your locla AI  during sessions.

Files here are git-ignored — your personal conversation context stays local.

## Generated files

- `*.md` — session memory files (created via /save command)
- `*.json` — structured memory snapshots
